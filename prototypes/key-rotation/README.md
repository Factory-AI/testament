# Key-rotation prototype

Informative, disposable research for `RES-PROTOTYPE-KEY-ROTATION-001`.
The runner rewraps one synthetic DEK under two synthetic roots and proves the
wrapped bytes change while source identity and payload ciphertext do not. It
makes no cloud KMS conformance claim.
